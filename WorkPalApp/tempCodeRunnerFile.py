#FOR SHOWING CPU USAGE#####################################################
        # self.cpu_usage_label = tk.Label(right_frame, text="CPU Usage:", bg="#f2f2f2", font=("Helvetica", 12))
        # self.cpu_usage_label.place(relx=0.5, rely=0.7, anchor="center")
        
        # self.cpu_usage_text = tk.StringVar()
        # self.cpu_usage_text.set("0%")
        
        # self.cpu_usage_value = tk.Label(right_frame, textvariable=self.cpu_usage_text, bg="#f2f2f2", fg="green", font=("Helvetica", 12))
        # self.cpu_usage_value.place(relx=0.6, rely=0.7, anchor="center")
        
        # # Update CPU usage label every 1 second
        # self.master = master
        # self.update_cpu_usage_label()## initializing function
        
        # self.master.after(1000, self.update_cpu_usage_label)

        # def update_cpu_usage_label(self):
        #     # Get the current CPU usage
        #     cpu_usage = psutil.cpu_percent(interval=0.1)
        #     self.cpu_usage_text.set(str(cpu_usage) + "%")
        
        #     # Update CPU usage label after 1 second
        #     self.master.after(1000, self.update_cpu_usage_label)

        # ################################################################################